검은색 클릭
파란색 리다이렉트
게시글 삭제 -> 화면에는 값이 N인 애들만 출력하기 / Y로 바꾼 것들은 출력x

-- 전체 조회
select *
from sample_boards
where board_deleted = 'N'
order by board_no desc

-- 페이징처리
-- 게시글 갯수 조회하기
select count(*)
from sample_boards
where board_deleted = 'N'
-- 해당 페이지번호의 게시글 조회
select *
from (select row_number() over (order by A.board_no desc) rn, A.*
      from sample_boards A
      where A.board_deleted = 'N')
where rn between #{beginIndex} and #{endIndex}


Board.java 클래스 정의
BoardDao.java 클래스 정의
boards.xml 파일 생성
mybatis-config.xml에 boards.xml 파일 등록

boards.xml
	게시글 목록조회하는 SQL문
	게시글 추가하는 SQL문